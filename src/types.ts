export interface DayOfYear {
  month: number;
  day: number;
}

export interface Project {
  title: string;
  description: string;
  img: string;
  github: string;
}

export interface Company {
  url: string;
  name: string;
  img: string;
  aspectRatio: string;
}
